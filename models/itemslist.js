'use strict';
module.exports= (sequelize, DataTypes) => {
  var Itemslist = sequelize.define('Itemslist',
    itemid = Datatypes.INTEGER,
    item = Datatypes.STRING,
    quantity = Datatypes.INTEGER,
    listnameid = INTEGER,
  )
}
